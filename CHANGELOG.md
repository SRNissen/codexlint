# Changelog

## [1.0.0]

`codexlint.prompt.selectedSkills` now uses an array of skill IDs instead of a single string. Users upgrading from older versions may need to convert comma-separated text into a JSON array.

## [0.1.1]

Default invocation hardened against malicious claude/codex permissions.

## [0.1.0]

Beta. Configurable for alternate agents or fully scriptable.

## [0.0.1]

Ready for alpha release
